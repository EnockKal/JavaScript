let age = 22;
if(age > 18){ // this is true so it go to the next line. if it was false it would have noit executed the block
    console.log("You are an adult") // it will print this line bcz the comparison statement was true
}

// an operation condition can be anything that can convert to a boolean

if ("smthg work"){
    "do smthg";
} else if("smthg else work"){
    "do smthing else";
} else{
    "then just do this"
}